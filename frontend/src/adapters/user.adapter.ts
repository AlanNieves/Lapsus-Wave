
/*import type { User as BackendUser } from "@/types";

export interface FrontendUser extends Omit<BackendUser, "avatar"> {
  imageUrl: string;
}

/**
 * Adapta el User que viene del backend al formato del frontend
 */
/*export function adaptUser(user: BackendUser): FrontendUser {
  return {
    ...user,
    imageUrl: user.avatar,
  };
}
*/